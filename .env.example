# jsFun Environment Configuration
# Copy to .env and modify as needed. All values below are defaults.

# Production server port (npm start)
PORT=3000

# Vite dev server port (npm run dev)
VITE_DEV_PORT=5173

# Express API port in dev mode (npm run dev:server)
VITE_API_PORT=3001
